#include <stdio.h>

/**
 * main - Prints the alphabet.
 * Description: 'get alphabets'
 * Return: always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 48; i < 58; i++)
	{
	putchar(i);
	}
	putchar('\n');
	return (0);
}
